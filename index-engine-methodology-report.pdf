/* ============================================================
   Jared Goroski — portfolio
   Dark-minimal. Deep navy base, single amber signal accent.
   Type: Space Grotesk (display) / Inter (body) / JetBrains Mono (data)
   ============================================================ */

:root {
  --bg:        #0B0E14;  /* deep navy-black */
  --surface:   #131926;  /* panels */
  --surface-2: #1A2233;  /* raised panels / hover */
  --border:    #28324A;  /* hairlines */
  --text:      #E6EAF2;  /* primary */
  --muted:     #8B95A7;  /* secondary */
  --faint:     #5A6478;  /* tertiary / out-of-index */
  --accent:    #E0A93B;  /* amber — "in index" signal */
  --accent-dim:#8A6B26;
  --link:      #6FA0F0;  /* interactive text */

  --maxw: 980px;
  --r: 10px;
}

/* ---------- light theme ---------- */
[data-theme="light"] {
  --bg:        #F6F7F9;  /* soft off-white */
  --surface:   #FFFFFF;  /* panels */
  --surface-2: #EDF0F5;  /* raised panels / hover */
  --border:    #D9DEE8;  /* hairlines */
  --text:      #161C28;  /* primary */
  --muted:     #54607A;  /* secondary */
  --faint:     #97A0B2;  /* tertiary */
  --accent:    #B5760F;  /* deeper amber — readable on light */
  --accent-dim:#9A7320;
  --link:      #2A5FC4;  /* interactive text */
}

* { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: "Inter", system-ui, -apple-system, sans-serif;
  font-size: 16px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

a { color: var(--link); text-decoration: none; }
a:hover { text-decoration: underline; }

::selection { background: var(--accent); color: #0B0E14; }

/* ---------- shared layout ---------- */
.wrap { max-width: var(--maxw); margin: 0 auto; padding: 0 24px; }

.eyebrow {
  font-family: "JetBrains Mono", ui-monospace, monospace;
  font-size: 0.72rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--accent);
}

section { padding: 72px 0; border-top: 1px solid var(--border); }
section:first-of-type { border-top: none; }

h2.section-title {
  font-family: "Space Grotesk", sans-serif;
  font-size: 1.5rem;
  font-weight: 600;
  letter-spacing: -0.01em;
  margin-bottom: 28px;
  color: var(--text);
}

/* ============================================================
   HERO
   ============================================================ */
.hero {
  position: relative;
  min-height: 78vh;
  display: flex;
  align-items: flex-end;
  overflow: hidden;
  border-top: none;
  padding: 0;
}

#indexCanvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

/* gradient so text stays legible over the field */
.hero::after {
  content: "";
  position: absolute;
  inset: 0;
  z-index: 2;
  background: linear-gradient(180deg,
    rgba(11,14,20,0.45) 0%,
    rgba(11,14,20,0.10) 40%,
    rgba(11,14,20,0.82) 100%);
  pointer-events: none;
}

[data-theme="light"] .hero::after {
  background: linear-gradient(180deg,
    rgba(246,247,249,0.25) 0%,
    rgba(246,247,249,0.04) 40%,
    rgba(246,247,249,0.88) 100%);
}

.hero-inner {
  position: relative;
  z-index: 3;
  width: 100%;
  max-width: var(--maxw);
  margin: 0 auto;
  padding: 0 24px 64px;
}

.hero h1 {
  font-family: "Space Grotesk", sans-serif;
  font-size: clamp(2.4rem, 6vw, 4rem);
  font-weight: 600;
  letter-spacing: -0.02em;
  line-height: 1.05;
  margin: 14px 0 16px;
}

.hero .lede {
  max-width: 52ch;
  font-size: 1.08rem;
  color: var(--muted);
}

.hero-meta {
  margin-top: 26px;
  display: flex;
  flex-wrap: wrap;
  gap: 10px 18px;
  font-family: "JetBrains Mono", ui-monospace, monospace;
  font-size: 0.78rem;
  color: var(--faint);
}
.hero-meta b { color: var(--text); font-weight: 500; }

/* legend for the constituent field */
.legend {
  position: absolute;
  z-index: 3;
  top: 20px; right: 24px;
  display: flex; gap: 16px;
  font-family: "JetBrains Mono", monospace;
  font-size: 0.68rem;
  color: var(--muted);
}
.legend span { display: inline-flex; align-items: center; gap: 6px; }
.legend .dot { width: 8px; height: 8px; border-radius: 50%; }
.legend .in  { background: var(--accent); }
.legend .out { background: var(--faint); }

/* ============================================================
   ABOUT
   ============================================================ */
.about p {
  max-width: 64ch;
  font-size: 1.08rem;
  color: var(--muted);
}
.about p + p { margin-top: 16px; }
.about strong { color: var(--text); font-weight: 600; }

/* ============================================================
   PROJECTS
   ============================================================ */
.project-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 18px;
}

.card {
  display: flex;
  flex-direction: column;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--r);
  padding: 24px;
  transition: border-color 0.2s ease, transform 0.2s ease, background 0.2s ease;
}
.card:hover {
  border-color: var(--accent);
  background: var(--surface-2);
  transform: translateY(-3px);
}

.card-eyebrow {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.68rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--faint);
  margin-bottom: 10px;
}

.card h3 {
  font-family: "Space Grotesk", sans-serif;
  font-size: 1.18rem;
  font-weight: 600;
  margin-bottom: 10px;
}
.card h3 a { color: var(--text); }
.card h3 a:hover { color: var(--accent); text-decoration: none; }

.card p { color: var(--muted); font-size: 0.95rem; flex-grow: 1; }

.metric {
  margin-top: 14px;
  padding: 10px 12px;
  border-left: 2px solid var(--accent);
  background: rgba(224,169,59,0.06);
  font-family: "JetBrains Mono", monospace;
  font-size: 0.78rem;
  color: var(--text);
  border-radius: 0 6px 6px 0;
}

.tags { margin-top: 16px; display: flex; flex-wrap: wrap; gap: 7px; }
.tag {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.7rem;
  color: var(--muted);
  border: 1px solid var(--border);
  border-radius: 5px;
  padding: 3px 8px;
}

.card-link {
  margin-top: 18px;
  font-family: "JetBrains Mono", monospace;
  font-size: 0.78rem;
  color: var(--accent);
}
.card:hover .card-link { text-decoration: underline; }

.draft { opacity: 0.92; }
.draft .card-eyebrow::after {
  content: " · in progress";
  color: var(--accent-dim);
}

/* ============================================================
   SKILLS
   ============================================================ */
.skill-cluster { margin-bottom: 26px; }
.skill-cluster:last-child { margin-bottom: 0; }
.skill-cluster h3 {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.74rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 12px;
}
.chips { display: flex; flex-wrap: wrap; gap: 8px; }
.chip {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 6px;
  padding: 6px 12px;
  font-size: 0.86rem;
  color: var(--text);
}

/* ============================================================
   PROJECT DETAIL PAGES
   ============================================================ */
.detail-header {
  border-bottom: 1px solid var(--border);
  padding: 28px 0 32px;
}
.back-link {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.78rem;
  color: var(--muted);
}
.detail-header h1 {
  font-family: "Space Grotesk", sans-serif;
  font-size: clamp(1.8rem, 4vw, 2.6rem);
  font-weight: 600;
  letter-spacing: -0.015em;
  margin-top: 16px;
}
.detail-sub {
  margin-top: 12px;
  font-family: "JetBrains Mono", monospace;
  font-size: 0.8rem;
  color: var(--faint);
}

.detail { max-width: 760px; margin: 0 auto; padding: 8px 24px 80px; }
.detail h2 {
  font-family: "Space Grotesk", sans-serif;
  font-size: 1.35rem;
  font-weight: 600;
  margin: 40px 0 12px;
}
.detail h3 {
  font-size: 1.05rem;
  font-weight: 600;
  color: var(--text);
  margin: 26px 0 8px;
}
.detail p { color: var(--muted); margin-bottom: 14px; }
.detail ul { color: var(--muted); margin: 0 0 16px 22px; }
.detail li { margin-bottom: 6px; }
.detail img {
  max-width: 100%;
  border: 1px solid var(--border);
  border-radius: 8px;
  margin: 18px 0;
}

/* placeholder block for not-yet-added screenshots */
.placeholder {
  border: 1px dashed var(--border);
  border-radius: 8px;
  padding: 28px;
  text-align: center;
  color: var(--faint);
  font-family: "JetBrains Mono", monospace;
  font-size: 0.8rem;
  margin: 18px 0;
}

/* captioned diagram/figure on detail pages */
.shot { margin: 18px 0; }
.shot img { margin: 0 0 8px 0; display: block; }
.shot figcaption {
  color: var(--faint);
  font-size: 0.82rem;
  line-height: 1.45;
}

/* ============================================================
   FOOTER + MODAL
   ============================================================ */
footer {
  border-top: 1px solid var(--border);
  padding: 30px 0;
}
.social-links {
  max-width: var(--maxw);
  margin: 0 auto;
  padding: 0 24px;
  display: flex;
  gap: 24px;
  align-items: center;
  font-family: "JetBrains Mono", monospace;
  font-size: 0.85rem;
}
.social-links a, .social-links button {
  color: var(--muted);
  background: none;
  border: none;
  cursor: pointer;
  font: inherit;
}
.social-links a:hover, .social-links button:hover { color: var(--accent); text-decoration: none; }
.social-links i { margin-right: 7px; }

.modal {
  display: none;
  position: fixed; inset: 0; z-index: 1000;
  background: rgba(0,0,0,0.6);
}
.modal-content {
  background: var(--surface);
  border: 1px solid var(--border);
  max-width: 380px;
  margin: 18vh auto 0;
  padding: 28px;
  border-radius: var(--r);
  text-align: center;
  position: relative;
}
.modal-content p { color: var(--muted); }
.modal-content #email-address { color: var(--text); font-family: "JetBrains Mono", monospace; }
.close-button {
  position: absolute; top: 10px; right: 16px;
  color: var(--faint); font-size: 26px; cursor: pointer; line-height: 1;
}
.close-button:hover { color: var(--text); }
#copy-button {
  margin-top: 16px; padding: 9px 18px;
  background: var(--accent); color: #0B0E14;
  border: none; border-radius: 6px; cursor: pointer;
  font-weight: 600;
}
#copy-button:hover { filter: brightness(1.08); }

/* ============================================================
   ACCESSIBILITY + RESPONSIVE
   ============================================================ */
a:focus-visible, button:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

@media (max-width: 720px) {
  .project-grid { grid-template-columns: 1fr; }
  section { padding: 56px 0; }
  .legend { display: none; }
}

@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
}

/* ============================================================
   THEME TOGGLE (fixed, bottom-left)
   ============================================================ */
.theme-toggle {
  position: fixed;
  left: 18px;
  bottom: 18px;
  z-index: 900;
  width: 42px;
  height: 42px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--surface);
  color: var(--muted);
  border: 1px solid var(--border);
  cursor: pointer;
  font-size: 0.95rem;
  line-height: 1;
  box-shadow: 0 2px 12px rgba(0,0,0,0.22);
  transition: color 0.2s ease, border-color 0.2s ease,
              background 0.2s ease, transform 0.2s ease;
}
.theme-toggle:hover {
  color: var(--accent);
  border-color: var(--accent);
  transform: translateY(-2px);
}
[data-theme="light"] .theme-toggle { box-shadow: 0 2px 12px rgba(20,28,40,0.12); }
[data-theme="light"] ::selection { background: var(--accent); color: #fff; }
